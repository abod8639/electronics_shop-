import 'package:flutter/material.dart';
import 'supports_page.dart';

class HelpView extends StatelessWidget {
  const HelpView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SupportsPage();
  }
}
